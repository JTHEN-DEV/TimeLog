import React from "react";

type Props = {};

const TimeTracker = (props: Props) => {
    return (
        <div>
            <h1>Time Tracker App</h1>
            <div>This is the actual time tracker application!!!</div>
        </div>
    );
};

export default TimeTracker;
